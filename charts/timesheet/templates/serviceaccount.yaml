{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ts.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ts.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end -}}
